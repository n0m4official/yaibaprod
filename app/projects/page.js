import Section from "@/components/Section";

export default function ProjectsPage() {
  return (
    <Section title="Projects">
      <p className="opacity-80">
        This is where we will add our film projects, for now it is empty so the team may see the website structure.
      </p>
    </Section>
  );
}